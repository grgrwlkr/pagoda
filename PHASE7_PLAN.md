# Фаза 7: Файлы и вложения

## Статус: В процессе

## Задачи

### 7.1 Загрузка файлов

**Функциональность:**
- ✅ Базовый endpoint для загрузки файлов (уже есть в messenger.go)
- ⚠️ Поддержка множественной загрузки
- ⚠️ Предпросмотр изображений
- ⚠️ Прогресс-бар загрузки
- ⚠️ Валидация типов файлов
- ⚠️ Ограничение размера файлов

**Endpoints:**
- `POST /message/:id/attachments` - загрузка файла к сообщению (уже есть)
- `POST /dm/:id/attachments` - загрузка файла к DM сообщению (нужно добавить)
- `GET /attachment/:id` - просмотр/скачивание файла (уже есть)
- `DELETE /attachment/:id` - удаление файла (уже есть)

**Реализация:**
- Использовать существующий `afero.Fs` из Container
- Организация файлов по workspace/channel или user/dm
- Проверка MIME-типов
- Ограничение размера (из конфига или по умолчанию 10MB)

### 7.2 Хранение файлов

**Функциональность:**
- ✅ Использовать `afero.Fs` из Container
- ⚠️ Организация по workspace/channel
- ⚠️ Организация по user/dm
- ⚠️ Ограничение размера файлов
- ⚠️ Проверка MIME-типов

**Структура хранения:**
```
uploads/
  workspace-{id}/
    channel-{id}/
      {attachment_id}_{filename}
    dm-{id}/
      {attachment_id}_{filename}
```

### 7.3 Отображение файлов

**Компоненты:**
- ⚠️ Компонент для отображения изображений (встроенный просмотр)
- ⚠️ Компонент для отображения документов (PDF, текстовые файлы)
- ⚠️ Компонент для отображения других типов файлов (ссылка на скачивание)
- ⚠️ Галерея файлов канала
- ⚠️ Список файлов в сообщении

**Реализация:**
- Определить тип файла по MIME-type
- Для изображений: показать превью
- Для PDF: встроенный просмотр (если возможно)
- Для других: иконка типа файла + ссылка на скачивание
- Компонент `FileAttachment` для отображения вложений в сообщениях

### 7.4 UI для загрузки файлов

**Компоненты:**
- ⚠️ Кнопка загрузки файлов в `MessageInput`
- ⚠️ Drag & drop для загрузки файлов
- ⚠️ Прогресс-бар загрузки
- ⚠️ Предпросмотр загружаемых файлов
- ⚠️ Множественная загрузка

**Реализация:**
- Интегрировать в `MessageInput` компонент
- Использовать HTMX для загрузки
- Показывать прогресс через HTMX или WebSocket
- Предпросмотр перед отправкой

---

## План реализации

### Шаг 1: Улучшить существующие endpoints
- Проверить и улучшить валидацию файлов
- Добавить проверку размера
- Добавить проверку MIME-типов
- Улучшить организацию файлов

### Шаг 2: Добавить endpoint для DM attachments
- Создать `POST /dm/:id/attachments`
- Аналогично message attachments

### Шаг 3: Создать компоненты для отображения файлов
- `FileAttachment` - компонент для отображения одного файла
- `FileGallery` - галерея файлов канала
- Определить типы файлов и соответствующие компоненты

### Шаг 4: Интегрировать загрузку в UI
- Добавить кнопку загрузки в `MessageInput`
- Реализовать drag & drop
- Добавить прогресс-бар
- Предпросмотр файлов

### Шаг 5: Улучшения
- Галерея файлов канала
- Список всех файлов в workspace
- Поиск файлов (опционально)

---

## Технические детали

### Конфигурация

Добавить в `config/config.yaml`:
```yaml
messenger:
  maxFileSize: 10485760  # 10MB
  allowedFileTypes:
    - "image/*"
    - "application/pdf"
    - "text/*"
    - "application/msword"
    - "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
```

### MIME-типы

**Изображения:**
- `image/jpeg`, `image/png`, `image/gif`, `image/webp`

**Документы:**
- `application/pdf`
- `text/*`
- `application/msword`
- `application/vnd.openxmlformats-officedocument.*`

**Другие:**
- Все остальные - показывать как ссылку на скачивание

### Структура компонентов

**FileAttachment:**
```go
type FileAttachment struct {
    ID       int64
    Filename string
    MimeType string
    FileSize int64
    URL      string
}
```

---

**Дата начала**: 2024-12-03
**Дата завершения**: 2024-12-03
**Статус**: ✅ Все задачи выполнены

## Выполненные изменения

### Конфигурация
- ✅ Добавлен `MessengerConfig` в `config.go`
- ✅ Настройки в `config.yaml` (maxFileSize, allowedFileTypes)

### Валидация файлов
- ✅ Проверка размера файла
- ✅ Проверка MIME-типа (с поддержкой wildcards)
- ✅ Функция `matchesMimeType` для проверки типов

### Endpoints
- ✅ `POST /message/:id/attachments` - улучшена валидация
- ✅ `POST /dm/:id/attachments` - новый endpoint для DM
- ✅ `GET /attachment/:id` - просмотр файлов
- ✅ `DELETE /attachment/:id` - удаление файлов

### UI компоненты
- ✅ Компонент `FileAttachment` для отображения вложений
- ✅ Поддержка разных типов файлов (изображения, PDF, текстовые, другие)
- ✅ Интеграция в `MessageList` для отображения attachments в сообщениях
- ✅ Загрузка файлов в `MessageInput` (кнопка, drag & drop)
- ✅ Предпросмотр загружаемых файлов (изображения показываются как превью)
- ✅ Множественная загрузка файлов

### Интеграция
- ✅ Attachments загружаются вместе с сообщениями
- ✅ HTMX для отправки сообщений с файлами
- ✅ Drag & drop для загрузки файлов

